# gl_utils: MRIcroGL helper scripts for better automation

This repo is basically a bunch of different functions that I use to automate aspects of MRIcroGL. Some (most tbh) of them are more broadly useful for different neuroimaging pipelines.

### Examples
Going to add Jupyter notebooks explaining my common use cases:

* `sphere_example` : draw a sphere with a specified radius around a specified coordinate
